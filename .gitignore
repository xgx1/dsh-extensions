node_modules/

# vendor/ 与 skills/ 下的内容**不再是忽略项**——它们已登记为 git submodule，
# 由 .gitmodules 记录（见 MyAI docs/adr/0005）。本仓库只保存指向各自提交的指针，
# 内容在子仓里提交、推送。
#
# 历史：vendor/ 曾用 ignore 规避「`git add -A` 把嵌套仓记成不含内容的 gitlink」，
# 2026-09-13 改用显式 submodule 正面解决同一问题。
